import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.virtual.system',
  appName: 'Virtual Fitting System',
  webDir: 'dist'
};

export default config;
